% ============================================================
% HEXIS — ICLR 2027 submission (Track B, dispositional axis)
% "The Prompt Is Not a Place to Keep a Belief"
%
% Style kit: ICLR 2027 official (media.iclr.cc), fetched 2026-08-19.
% If the 2027 kit is later revised/republished, swap style/*.sty
% and style/*.bst for the updated versions and recompile — no
% section-file changes should be required.
% ============================================================
\documentclass{article}

% Full-length build: all sections, including the S7.2-7.4 isolation
% ablations and the related-work section, are gated \iflongversion true.
\newif\iflongversion
\longversiontrue

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{style/iclr2027_conference,times}

\input{style/math_commands}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{url}
\usepackage{natbib}
\usepackage{xcolor}
\usepackage{subcaption}
\usepackage{microtype}
\usepackage{multirow}
\usepackage{pifont}
\usepackage{enumitem}
\usepackage{bm}
\usepackage{float}
\usepackage[section]{placeins}
\usepackage{longtable}
\usepackage{array}

\newcommand{\cmark}{\ding{51}}
\newcommand{\xmark}{\ding{55}}

% Condition-letter macros — confine letters to tables per outline
% terminology pass (stated / compiled in prose, letters in tables only).
\newcommand{\condA}{\textsc{bare}}
\newcommand{\condB}{\textsc{ctx}}
\newcommand{\condC}{\textsc{cmp}}
\newcommand{\condD}{\textsc{ctx{+}cmp}}
\newcommand{\condF}{\textsc{cmp{+}slot}}

% Experiment cross-reference macro. Each registry row in
% Appendix~\ref{app:expindex} (sections/appendix.tex) carries both a
% \defcodeonly{label}{CODE} (defines the printable code, sections/expcodes.tex
% — shared with workshop.tex, which has no appendix) and its own
% \label{app:exp-<code>} at the row itself (so the hyperlink lands on the
% row, not merely somewhere in the document). \exprefcode extracts the code
% string; \expref renders a clickable "(Exp. C.2b)"-style tag.
\makeatletter
\newcommand{\exprefcode}[1]{%
  \@nameuse{expcode@#1}%
}
\newcommand{\defcodeonly}[2]{%
  \@namedef{expcode@#1}{#2}%
}
\makeatother
\newcommand{\expref}[1]{\hyperref[#1]{(Exp.~\exprefcode{#1})}}

\title{\Large Belief or Bias? Measuring Epistemic Interventions\\on Conviction and Discrimination}

% ICLR: double-blind — no author info at submission.
% Uncomment \iclrfinalcopy in style/iclr2027_conference.sty usage below
% for camera-ready only; do not add author names here before then.
\author{Anonymous authors \\ Paper under double-blind review}

\newcommand{\fixme}[1]{\textcolor{red}{\textbf{[FIXME: #1]}}}
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

\begin{document}
\vspace*{-0.4in}
\maketitle

% Experiment-code label definitions (shared with workshop.tex) — must load
% before any \expref call in the body. Appendix~\ref{app:expindex}'s
% registry table (sections/appendix.tex) is what these labels actually
% point to.
\input{sections/expcodes}

\input{sections_submit/abstract}
\input{sections_submit/intro}
\input{sections_submit/related}
\input{sections_submit/measurement}
\input{sections_submit/dissociation}
\input{sections_submit/isolation}
\input{sections_submit/discrimination}
\input{sections_submit/boundary}
\input{sections_submit/discussion}
\input{sections_submit/future_work}
\input{sections_submit/objections}
\input{sections_submit/limitations}
\input{sections_submit/conclusion}
\input{sections/statements}

\bibliographystyle{style/iclr2027_conference}
\bibliography{references}

\appendix
\input{sections/appendix}

\end{document}
